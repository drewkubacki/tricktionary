class Trick {
  String name;
  String description;
  String image;
  int difficulty;

  Trick({
    required this.name,
    required this.description,
    required this.image,
    required this.difficulty,
  });
}
